{% extends '@admin/default_frame.twig' %}

{# {% set menus = ['plugin_social_login', 'config'] %} #}

{% block title %}{{ 'admin.firebase.title'|trans }}{% endblock %}
{% block sub_title %}{{ 'admin.firebase.sub_title'|trans }}{% endblock %}

{# {% form_theme form '@admin/Form/bootstrap_4_horizontal_layout.html.twig' %} #}

{% block stylesheet %}{% endblock stylesheet %}

{% block javascript %}{% endblock javascript %}

{% block main %}
    <form role="form" method="post">

        {# {{ form_widget(form._token) }} #}

        <div class="c-contentsArea__cols">
            <div class="c-contentsArea__primaryCol">
                <div class="c-primaryCol">
                    <div class="card rounded border-0 mb-4">
                        <div class="card-header">
                            <span>{{'admin.firebase.delete_user.title'|trans}}</span>
                        </div>

                        <div class="card-body">
                            <div class="row mb-2">
                                <div class="col-12">
                                    <span>{{'admin.firebase.delete_user.label'|trans}}</span>
                                </div>
                            </div>
                            <div class="row">
                                <div class="col-3">
                                    {{ form_widget(form.firebase_delete) }}
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </form>
{% endblock %}
